{
  "mcpServers": {
    "container": {
      "command": "container-mcp",
      "args": ["--transport", "stdio"],
      "env": {
        "CONTAINER_URL": "${CONTAINER_URL:-http://localhost:8080}",
        "CONTAINER_TOKEN": "${CONTAINER_TOKEN}"
      }
    }
  }
}
